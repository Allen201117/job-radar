# 核心功能对抗性审查（2026-09-17）：审出即修，修完对拍

> 创始人定的范围：两个核心价值（精准可靠的岗位推荐 / 有效的洞察）延伸出的功能块——岗位库全生命周期与抓取效率、
> 推荐与筛选精度、洞察库供给与匹配、公告制招聘、非互联网行业与中小企业、简历解析与匹配。
> 定的四条：P1 正确性直接修 + 架构级出 spec 派单；每个修复验证全绿后直接合 main；前四轮审查未修项先核验再纳入；
> 推荐算法只抄成熟设计、不上新基建。期间追加两条：金融秋招覆盖远远不够；必投 30 家的抽取逻辑要说清。
>
> 方法：主线程读推荐链全部代码并亲自做真库对拍；5 路 Sonnet 子 agent 扫全集取证（历史未修项核验 / 洞察 / 公告制 /
> 爬虫仪表 / 简历链）+ 1 路联网调研 + 2 路 Chrome 前端走查（创始人已登录的 Chrome）+ 1 路银行券商校招入口调研；
> Python 侧机械修复两包甩给 Codex（TDD、变异验证、2247 / 2241 单测全绿）。所有数字都是当天真库 / 线上实测。

## 0. 结论三句话

1. **前四轮审查（09-03/06/07/08）的 24 条缺陷里 6 条确认已修、2 条部分修、其余本轮修掉了 9 条**；仍开着的只有
   F3 浏览器巡检 unsure 盖戳（要新列牵动轮转）、F7/R08 备份演练（非代码）、R09 用户价值观察（产品运营）。
2. **推荐链的病根不在打分，在召回窗口**：方向层命中集远大于份额时按「城市→最新」截断，精确标题命中被砍在窗外；
   非互联网画像连方向层都命中不了（词库是互联网口径）。加了 `job_function` 第四层 + 标题优先 + 城市门后，
   18 个画像 13 升 4 降 1 平，长尾从无到有（土木 0→3、教师 3→6、机械 20→40）。
3. **金融秋招覆盖确实远远不够**：金融必投 30 家里校招岗为 0 的有 10 家（招行 / 民生 / 网商 / 微众 / 中信银行 / 中信证券 /
   中国银行 / 兴业 / 宁波 / 京东科技），全库带「银行」的公司有校招岗的只有 10 家、其中 5 家国有大行占 99%。
   已查清 12 家的官方入口与平台，两家（华泰证券、微众银行校招）只需插源、零代码。

## 1. 本轮修掉的（已合 main）

| # | 模块 | 缺陷（现象 → 根因） | 修法 | 证据 |
|---|---|---|---|---|
| 1 | 洞察·归属 | 「京东」子串命中「京东方」、「中国银行」命中「中国银行业协会」，BOE 的岗算进京东派生指标 → `eligibleForSubstring` 只看「含汉字」 | 三层规则：括号集团名 / 装饰词全剥（含品牌+行业描述词）/ 地名+组织单元；子公司靠 aliases | 全集对拍：1,383 画像 × 1,537 家在招公司（见 §3.1） |
| 2 | 洞察·撤回 | 申诉成立/下架后 /insights 索引缓存不失效，`invalidateCompanyInsights` 是死函数（I5 残差） | 接口 `revalidateTag`，管理员端清抽屉缓存 | tests/insight-dispute-resolve |
| 3 | 洞察·写入 | 三表非原子，来源写失败留孤儿 active item，且被算进 `written_dims` → 上一轮有效版本被退役（I3） | 补偿删除 + 抛出，主题失败不计入换代 | Codex TDD + 变异验证 |
| 4 | 洞察·复核戳 | 部分主题跑完后异常仍盖 `t3_checked_at`（排除 180 天）（I1 残留） | 只有至少一个主题完整走完才盖 | 同上 |
| 5 | 简历·英文 | `en_target_keywords` 是 `en_skills` 副本 → 没填英文岗位时技能当方向；英文档案从不写 `user_preferences` → /jobs 海外排序读不到 | 副本改空；迁移 253 补列 + 保存时两处同写 + 存量回填 | tests/resume-en-mapping |
| 6 | 简历·方向 | LLM 可把 Java/Spring 写进 `target_roles`，无值域门 → 所有含 Java 的岗拿方向分 | prompt 硬禁 + 归一化门（同时在 skills 里且判不出职能才剔） | tests/resume-extract-normalization |
| 7 | 简历·上传 | 扫描版 PDF 抽取失败被当成「文件类型不支持」415 | 抽取失败单独 422 并说清原因 | route |
| 8 | 公告制·截止日 | 认不出「截止日期：9月20日」「报名截止：9月20日」等最常见写法；12 月发布 1 月截止直接判过期 | 补 3 条明确截止语义 pattern；跨年进位 | Codex TDD |
| 9 | 公告制·分类 | `_EXCLUDE` 裸「说明」误杀「招募条件说明公告」 | 只排除补充/操作/问题/情况说明 | 同上 |
| 10 | 公告制·台账 | 单 portal 0 候选不出声；Mac 主 runner（23 省）宕机无人知 | 逐 portal warning + `zero_found_portals`；metrics 带 runner；看门狗规则 M（30h 无 mac 记录） | 同上 |
| 11 | 爬虫·台账 | `crawl_run_unrecorded` 只在会被截断的 CI 日志里 | 看门狗规则 N | 同上 |
| 12 | 爬虫·抓全 | jd.py 短页直接 `fetch_complete=True`（限流回短页就把没翻到的判成抓全）；`coverage_complete=False` 的源照记 success（R02） | 短页只 break；不完整记 partial_success，消费方逐个核过口径不变 | 同上 |
| 13 | 爬虫·浏览器 | `playwright_base` 全体子类没装 dialog 守卫（09-13 红线只修了巡检） | 抽出共享 `install_dialog_guard` 装进 fetch() | 同上 |
| 14 | 推荐·分类口径 | /today 召回不带 `recruitment_category` 列，用 300 字摘要重算 → 社招岗匹配理由写「校招岗位」（线上实测） | `classifyJobFunction` / `recruitmentCategory` 有物化列就认列；召回与 /jobs 候选都带列 | 68 条 matcher 单测 |
| 15 | 推荐·召回 | 见 §2 | 第四层 function + 标题优先 + 城市门 + 复合索引 | 18 画像对拍 + EXPLAIN |
| 16 | 推荐·散列 | /today 前 8 张全是字节跳动：配额只限总数不限相邻 | main 区滑窗散列（6 张内同一家 ≤2） | tests/opportunity-grouping |
| 17 | /jobs·冷路径 | 默认态实测 35s（走查）：2.8 万行正文 20 MB 全传 | 正文按需传 + 排除词下推 SQL + 命中页回补摘要 | 20 MB → 4.1 / 7.7 / 0 MB；线上 TTFB 见 §3.3 |

## 2. 推荐链：第一性原理下的判断

现行是标准的两阶段（SQL 召回 → JS 硬门 + 加权打分 → 分区 + 配额），与 LinkedIn L1/L2、ZipRecruiter 多阶段专利的形态一致；
调研（researcher，来源见文末）确认在「无行为数据 + 显式偏好」的约束下，**先结构化硬门、再关键词、最后多样性**就是业内做法，
没有必要上向量库。真正的差距在三处，本轮改了前两处：

1. **召回窗口不按「该留谁」截断**（已修）。方向 tsquery 命中集 ≫ 份额时，窗口按城市→最新排，精确标题命中被砍。
   加「用户原词在标题里」作第二排序键（城市之后）。
2. **词库是互联网口径，非互联网画像召回为零**（已修）。`job_function` 是标题+正文分类好的物化列，加第四层
   `job_function = any(目标职能) AND search_doc @@ 城市`（BitmapAnd 两个索引，182~398ms）。三种反例都实测过并否掉
   （见 CLAUDE.md「/today 召回加了第四层」）。
3. **/today 与 /jobs 是两套打分**（未改，出 spec）：`lib/opportunities/scoring.ts`（方向 40/30/22、城市 15、阶段 10、行业 10、
   技能 ≤15、新鲜度线性）与 `lib/scoring.ts`（方向 30/15、城市 20、公司 15、关键词 5/2.5、新鲜度 10 二值），硬门也不同
   （/today 有阶段/学历/行业硬门，/jobs 没有）。同一个用户在两页看到两种排名。统一成一份 MatchFacts + 一份权重表是
   正确方向，但要用 match-eval 在两个页面同时验收，属单独立项。

对拍数字（scratchpad w2-measure，真实 stage-1 SQL + 真实 stage-2 引擎，18 个画像，可展示岗数 改前 → 改后）：

| 画像 | 改前 | 改后 | 画像 | 改前 | 改后 |
|---|---:|---:|---|---:|---:|
| AI产品经理@北京 | 698 | 1128 | 产品经理（跨行业）| 530 | 899 |
| 算法@杭州 | 936 | 843 | 机械工程师@上海 | 20 | 40 |
| 前端@深圳 | 126 | 131 | 临床护士@广州 | 11 | 12 |
| 数据分析@上海 | 541 | 475 | 银行柜员@深圳 | 2 | 2 |
| 市场运营@上海 | 843 | 776 | 中学教师@杭州 | 3 | 6 |
| 财务审计@北京 | 789 | 745 | 土木工程师@成都 | 0 | 3 |
| 6 个校招画像 | 23~371 | 全部 +5~+16 | | | |

双向：13 升 / 4 降 / 1 平。4 个降的（−44~−93）各仍 ≥475，远超每日 20~30 张，是方向层份额从 5/8 降到 5/12 的代价。
top-20 精确标题占比不变（20/20；机械 18→19）。in-DB 耗时 127~802ms（改前两例 250/287ms）。
「相关档压过精确档」的倒挂在 top-20 里为 0，这个怀疑作废。

## 3. 验证

### 3.1 洞察公司匹配全集对拍
`company_profiles` 全部画像 × 香港库全部在招公司名，逐对跑旧/新 `companyMatches`（2026-09-17 实跑）：

| 指标 | 数字 |
|---|---:|
| 画像数 × 在招公司名 | 1,383 × 1,537 |
| 旧新都命中（保留） | 1,554 |
| 旧命中、新不命中（dropped） | 64 |
| 旧不命中、新命中（added） | **0** |

第一版规则（「多出部分必须全是装饰词」）dropped=434，其中大量是同一家的真实写法（云南白药 YNBY 校招 / 中铁十七局（中国铁建）/ 中国电建集团江西省水电工程局），因此改成三层规则 + 品牌行业描述词（创新/出行/快递/速递/直播/企业/乳业/饮料/找房/前海）。
最终 64 对 dropped 逐条人工看过：网易有道·网易云音乐·京东物流·阿里云·腾讯音乐·中通云仓/冷链/餐饮·华峰测控·东软医疗·吉利德·京东方·中国建研院系四家·中国石油和化学工业联合会 等，全部是独立法人或子公司（归集团要靠 aliases 显式登记）。
诚实边界：刻意不收「金融 / 物流 / 音乐 / 云 / 证券 / 汽车」，代价是「度小满金融科技（北京）」不再归度小满、「吉利汽车」不再归吉利——需要时加 alias，不放宽子串。
判据：新增命中必须为 0（纯收紧）；掉的逐条人工看是不是「另一家公司」。

### 3.2 单测 / 构建
- node --test：1,271 → 1,2xx 全绿（本轮新增/改动 9 个测试文件，含契约：正文回补列必含 summary、function 层不许 or-null 门）。
- crawler unittest：2,247 全绿；`git diff --check` 通过；ESLint（`--no-eslintrc -c .eslintrc.json`，绕开 worktree 双配置冲突）0 问题；`next build` 通过。

### 3.3 线上
2026-09-17 部署（main 385c267，Vercel status success、迁移 253 已 apply）后实测：

| 请求（匿名 curl，hkg1） | 改前（09-08） | 改后 |
|---|---:|---:|
| `sortBy=match&limit=10` 冷 | 14.1~16.6s | **12.96s / 10.77s**（两次冷） |
| 同上，同实例复打 | 0.56s | 0.58s |
| `sortBy=newest&limit=10` | 3.25s | 4.02s |
| `sortBy=match&city=北京` | 4.37s | 3.61s |

结论：匿名冷路径只降 ~20%，**没有出现「正文 0 传输」应有的量级下降**。说明剩余大头不在 summary 传输，而在 28k 候选的其余列 + JS 打分（§4「/jobs 冷路径的另一半」），下一步应先在函数里打分段耗时日志再动。
诚实边界：无法从外部证明线上函数已跑新代码（Vercel 状态时间戳恒等于创建时间、chunk hash 本地与 Vercel 不一致），依据只有 GitHub 状态 success + 无 failure。

**分段账本（同日加 `x-jobs-search-timing` 头后实测，匿名，hkg1）：**

| 请求 | rows | fetch | score | tail | TTFB |
|---|---:|---:|---:|---:|---:|
| match（默认态） | 28,000 | 7,952 / 8,323 | 335 / 278 | 12 / 2,666 | 8.96s / 12.2s |
| newest | 1,000 | 2,715 | 9 | 3 | 3.08s |
| match + 北京 | 8,000 | 3,921 | 97 | 295 | 4.68s |
| match + 关键词 | 8,000 | 7,177 | 248 | 11 | 7.79s |

**打分只占 3%，96% 在「取数」；而 1,000 行也要 2.7s ⇒ 固定开销 ~2.5s + 每 MB 极慢。**
裸吞吐实验（psql 从本机拉 `repeat('x', N)`）：1MB 3.6s / 4MB 4.5s / **8MB 42.9s（1.5 Mbit/s）**，`select 1` 连接+往返 1.66s。
→ **根因是香港轻量机的出口带宽上限（个位数 Mbps，突发额度用完后更低），不是 SQL、不是函数区域、不是 JS。**
此前「砍 summary / 缓存候选 / 并行取页 / json_agg」全部是在给细水管做节流，且解释了历史上「并行取页反而更慢」「25s→35s 漂移」等所有反常现象。
根治只有两条：① 把「取 2.8 万行进函数」这个形态整个去掉（打分下推 SQL，只回 1 页）；② 提带宽 / 把计算搬到库同机。见 §7。

/today（创始人账号，前台 tab）：「对口机会」前 8 张公司 = 字节 / 字节 / 得物 / 得物 / 阿里 / 小红书 / 沐瞳 / 字节 —— 不再连续 8 张字节，符合 cap2/window6。30 张理由标签与「招聘类型」一致（全部校招）。残留：阿里「Maas 平台 AI 商业化产品经理」正文写「招聘项目：日常实习生」但类型标校招（分类器把阿里日常实习归校招，属另一问题）。

## 4. 没修、出 spec 或任务卡的

| 项 | 为什么不在本轮直接修 | 建议 |
|---|---|---|
| /today 与 /jobs 两套打分统一 | 跨两页的排序语义变更，要 match-eval 双页验收 | 单独立项（见 §2.3） |
| F3 浏览器巡检 unsure 盖戳 | 要加 `liveness_attempted_at` 列并改轮转（按 coalesce 取最旧），否则 unsure 岗会被反复插队 | 迁移 + `audit_dead_links.mark()` 分三态 |
| /jobs 冷路径的另一半 | 正文按需传后瓶颈转到 JS 打分 2.8 万行；再进一步要物化 `job_role`/最低年限 | 先看线上 TTFB 再定 |
| 求职范围切换不改 /jobs 计数（城市筛选存在时） | 走查发现，未定位 | 单独查 `effectiveJobScope` 与 URL 筛选态的关系 |
| /jobs 统计条「企业官方源 / 24h 核验」恒「—」 | 未定位 | 查 /api/jobs/stats 两个字段 |
| 排序开关藏在「更多」 | 交互决策 | 提到主 filter bar |
| 公告制 `_INCLUDE` 的「引进」在综合栏目收进方案/会议通知 | 需逐省实测才能收紧 | 观察 |
| antgroup / mihoyo / tencent_music 多渠道 sum 分母 | 需 live 验证渠道是否重叠 | 逐家对拍后按 huawei_campus 改逐渠道 drained |

## 5. 创始人两个追问

**金融秋招覆盖。** 真库：全库校招在招 75,071（7 天新增 4,739、30 天 47,423、982 家）。金融必投 30 家里校招岗：
建行 3,784 / 农行 2,670 / 工行 2,569 / 人保 2,083 / 人寿 1,704 / 泰康 623 / 交行 536 / 浦发 328 / 蚂蚁 206 / 东方财富 98 /
同花顺 56 / 国信 52 / 广发 46 / 招商证券 27 / 中金 26 / 其余 ≤9，**10 家为 0**。银行券商官方校招入口逐家查清（researcher，
详见 docs/reviews/2026-09-17-evidence/finance-campus-portals.md）：
1. 华泰证券 `wecruit.hotjob.cn/SU6419745cbef57c635fe10142/pb/school.html`、微众银行校招 `wecruit.hotjob.cn/SU61027bb10dcad47a7e23e040/pb/school.html`
   ——`hotjob.py` 零改动，只需插源（探活门过了才留）。
2. 中信证券 `sec.hotjob.cn/wt/SEC/web/index/campus`——`wt.py` 可复用，需 CI 验通。
3. 网商银行——蚂蚁 campus board 已在 `antgroup.py` 里，0 校招要查是标题不含「网商银行」还是真没岗。
4. 招行 / 民生：现有 `cmb.py` / `cmbc.py` 扩校招分支（各 ~30 行，需抓包定接口）。
5. 兴业（wt，geo-block 需 CI 验）、光大（`/uiap/wt/CEB/` 前缀要覆写 `_bind_source`）。
6. 宁波 / 平安 / 中信银行：平台未知，从零调研。中国银行：官网无逐岗页、网申在中华英才网（第三方红线），保持公告制处理。

**必投 30 家怎么选的。** 代码事实：`lib/must-apply-list.json` 是 **11 行业 × 30 家手工精选头部公司**的静态清单（2026-07-14 立），
按用户简历行业 `canonicalizeUserIndustry` 取对应一份，不按简历适配度选。它的角色是**供给侧北极星**（这些雇主我们有没有货）
+ 扩源目标，/campus 按它展示 30 张卡。所以「30 家最大的」是对的，「30 家最适合用户的」不是它在回答的问题。
建议（未做，出 spec）：北极星清单保持静态；用户侧另派生一份「对你有货的必投公司」= 清单 ∩ {有该用户可投岗的公司}
（按用户职能 / 城市 / 阶段用现有 `job_function` / `recruitment_category` 列过滤，按可投岗数 + 新鲜度排），
这样 /campus 卡面不再给校招用户看一家当前 0 校招的银行。

## 6. 改了 CLAUDE.md 的哪几段（创始人可否决）
- 「/jobs 默认排序冷路径」段末追加 📌 2026-09-17「正文按需传」。
- 新增「/today 召回加了第四层 function」一节（含三种否掉的写法与复合索引）。
- 「模块 B 职业洞察层」红线追加「公司归属的中文子串门」。

调研来源（researcher）：LinkedIn Recruiter AI 博客（L1/L2、canonical matching）、ZipRecruiter 多阶段候选排序专利、
RRF（BigDataBoutique）、Fifty Shades of Ratings（负反馈硬过滤）、Indeed RecSysHR2024 伪在线召回评测、NLP4HR 综述。

## 7. 创始人复审后的根因重排（2026-09-17 晚，「不要治标不治本」）

| 表象 | 本轮治标做法 | 真正的根 | 根治方向 |
|---|---|---|---|
| /jobs 10s+、/today 慢 | 砍正文、缓存、第四层召回 | **每次请求把 2.8 万行拖进函数打分** + 香港机出口带宽个位数 Mbps | 匹配事实全部物化成列，打分/排序在 SQL 里做，一页只回 60 行；带宽是硬件项要创始人拍板 |
| /today 与 /jobs 排名不一致 | 各自修 bug | 两套打分、两套硬门、两套词库 | 一份 MatchFacts + 一份权重表，两页读同一条 SQL |
| 洞察张冠李戴 | 子串门三层规则 | 靠公司名字符串在读取时猜归属 | `company_id` 实体表，source→实体一次映射，插入时定归属，洞察按 id join |
| 简历字段错位 | 逐字段补 | 画像与岗位不共用一套枚举；档案存两处 | 简历抽取受岗位侧同一套枚举约束（职能 / 城市 / 阶段），档案单一事实源 |
| 金融校招 10 家为 0 | 查了入口没接 | 源模型是「一家公司 = 一个 slug」，没有「渠道」概念；缺口普查只看有没有源、不看有没有校招渠道 | 必投公司「渠道登记表」（社招 / 校招 / 实习各一条 URL + 平台 + 探活状态），旺季校招渠道缺失 = 告警 |
| 必投 30 家不贴用户 | 说明口径 | 一份清单同时当「供给北极星」和「给用户看的」 | 拆成两份：静态北极星 + 「对你有货」派生视图 |
| 公告制过期/误杀 | 加正则 | 靠正则从异构公告里抽结构 + 定时任务押在一台 Mac 上 | 抽取失败走 LLM 结构化兜底（有 schema、有校验、有成本闸）；大陆 runner |

## 8. 根因方案落地进度（2026-09-17 晚）

| 步 | 状态 | 证据 |
|---|---|---|
| ① 校招渠道账本 + 按渠道的缺口指标 | ✅ 上线（main 28c4674，迁移 254） | 线上台账 healthy 187 / idle 19 / missing 123；/admin/health 供给页三卡；看门狗规则 O（校招季 missing ≥5 告警）；`ops_runs.gap_funnel.metrics.campus_channel` 记趋势 |
| ② 按平台成批接校招渠道 | 🔄 第一簇 国聘 完成（main 8816527，迁移 255） | 17 家央企校招源用真实 adapter 逐家探活入库；11 家 0 岗刻意不写 |
| ②′ 顺手挖出的归属根因 | ✅ 修 | 国聘集团锚点「搜索第一行」→「集团简称/全称过核名」：28 源双向对拍 11 不变 / 1 纠正（中国建筑：建研院→中建）/ 8 错集团被拦 / 8 原本就展开失败 / 0 新增误挂 |
| ③ 公司实体表 | ⏸ 待创始人点头 | — |
| ④ 搜索下推 SQL / 带宽 | ⏸ 待创始人点头 | — |

**调研入口不可信的实例（别再直接用）**：华泰证券 wecruit 校招页自己写「在招职位 0 个」；`SU61027bb…` 是招联不是微众；`sec.hotjob.cn` 是上海电气不是中信证券。所有入口必须经真实 adapter 端到端探活 + 页面标题核对。

**下一簇（按现有源的平台分）**：beisen mixed 但 3 天零校招的 11 家（星巴克/潍柴/迈瑞/黑芝麻/三峡/喜马拉雅…）先查 `/campus` 路由是否被 adapter 抓到（潍柴 `/custom/xiaoyuan` 出的 259 岗 recruitment_category 全 NULL）；hotjob 4 家、moka 10 家、feishu 1 家推导兄弟 URL；90 家无源走缺口漏斗的「校招车道」（entry_finder 搜「{公司} 2027 校园招聘」，属单独改造）。

**遗留清理**：中国建筑社招源此前挂到中国建研院名下入库的岗（company 带「（中国建研院）」）、泰康/粉笔挂「（国投集团）」的岗，需用 `crawler/audit_iguopin_attribution.py` 复核并折叠。

