// /today 页头文案的**唯一**来源：page.tsx 与 loading.tsx（骨架屏）共用。
//
// 为什么单独抽出来：这段文案原本在两个文件里各写一份，2026-09-06 改页头时只改了 page.tsx，
// 骨架屏还挂着旧句子 —— 流式渲染下用户会先看到旧文案闪一下再被替换。文案要么一处都别重复，
// 要么迟早漂。
//
// ⚠️ 页头只写页面名，**不许写成一句宣称**。它在 Suspense 之外、先于「画像就绪与否」渲染，
// 而设过求职目标的人看到的是按目标精筛的机会、没设过的看到的是「热门在招」兜底
// （见 app/today-popular-client.tsx）——所以它不能声称「已按你的目标筛过」，对新用户那是假话，
// 而新用户恰恰是第一屏。具体是哪一种，由正文自己说清。
export const TODAY_HERO = {
  title: "推荐",
} as const;
